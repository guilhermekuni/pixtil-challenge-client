import { useState } from 'react';

import * as S from './styles';

const SearchBar = () => {
  return (
    <S.SearchBarWrapper>
      <h1>SEARCH</h1>
    </S.SearchBarWrapper>
  );
};

export default SearchBar;
